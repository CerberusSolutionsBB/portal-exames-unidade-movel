# GitHub Pages é hospedagem estática: não há backend para receber /api/acesso.
# Vazio = modo demonstração (validação no navegador, aviso de exame indisponível).
# Para ligar no backend real, preencha com a URL pública da API,
# ex.: VITE_API_BASE_URL=https://api.exemplo.com/api
VITE_API_BASE_URL=
